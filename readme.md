0. git remote -v =показывает ссылку на репозиторий
1.git status =посмотреть было ли изменение каких-либо файловп
2.git add [files] добавляет файлы в stage/промежуточную область ТОЧКА ДОБАВЛЯЕТ ВСЕ ФАЙЛЫ .
3.git commit -m "comment"=оставляешь комментарий 
4. git log/git log --oneline =показывает длинную/короткую запись о наших комментариях
5. git push [ссылка/rep_link] [ветка/branch_name]
6. git reset [название файла] = позволяет удалить файл из stage
7. git diff = позволяет посмотреть какую строчку кода ты добавил/удалил и где
8. git reser --hard = возвращает удаленную строки кода
.gitignore файл в котором мы можем писать названия файлов,папок которые не должны выгружаться на github
git branch =посмотреть ветки,ветки делаются для того чтобы делить код/разные страницы
git branch [название ветки]=создание новой ветки
git checkout [название ветки] = переключение между ветками